<%= form_with(model: user_saved_video, local: true) do |form| %>
  <% if user_saved_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_saved_video.errors.count, "error") %> prohibited this user_saved_video from being saved:</h2>

      <ul>
      <% user_saved_video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :video_id %>
    <%= form.text_field :video_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
